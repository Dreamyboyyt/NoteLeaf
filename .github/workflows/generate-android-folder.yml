name: Generate Android Folder

on:
  workflow_dispatch:

env:
  FLUTTER_VERSION: '3.32.3'

jobs:
  generate-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: Generate Android platform files
        run: flutter create . --platforms=android --org=com.sleepy.noteleaf

      - name: Create zip of Android folder
        run: |
          zip -r android-folder.zip android/

      - name: Upload Android folder artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-folder-noteleaf
          path: android-folder.zip
          retention-days: 7


      - name: Summary
        run: |
          echo "✅ Android folder generated successfully!"
          echo ""
          echo "📦 Artifact: android-folder-noteleaf.zip"
          echo "🎯 Package: com.sleepy.noteleaf"
          echo ""
          echo "📥 Download, extract, and edit locally as needed!"
